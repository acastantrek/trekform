import { useState } from 'react'
import { ArrowRight, Menu, Phone, ShieldCheck, X } from 'lucide-react'
import { NavLink } from 'react-router-dom'
import { Logo } from '../common/Logo'
const links = [{ to: '/', label: 'Inicio' }, { to: '/nosotros', label: 'Nosotros' }, { to: '/cursos', label: 'Cursos' }, { to: '/empresas', label: 'Empresas' }, { to: '/blog', label: 'Blog' }, { to: '/contacto', label: 'Contacto' }]
export function Header() { const [isOpen, setIsOpen] = useState(false); return <><div className="topline"><span><ShieldCheck size={16}/> Formación acreditada · Validez nacional</span><div><a href="tel:+34932640532"><Phone size={15}/> 93 264 05 32</a><span className="top-sep">L–J 9:00–18:00 · V 9:00–15:00</span></div></div><header className="nav-wrap"><Logo/><nav className={`nav${isOpen ? ' open' : ''}`} aria-label="Navegación principal">{links.map(link => <NavLink key={link.to} to={link.to} end={link.to === '/'} onClick={() => setIsOpen(false)} className={({ isActive }) => isActive ? 'active' : ''}>{link.label}</NavLink>)}<NavLink className="mobile-cta" to="/contacto" onClick={() => setIsOpen(false)}>Inscríbete ahora</NavLink></nav><NavLink className="nav-cta" to="/contacto">Inscríbete <ArrowRight size={17}/></NavLink><button className="menu" type="button" onClick={() => setIsOpen(value => !value)} aria-expanded={isOpen} aria-label={isOpen ? 'Cerrar menú' : 'Abrir menú'}>{isOpen ? <X/> : <Menu/>}</button></header></> }
